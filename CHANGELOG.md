Add an override to `simple` item to support new float list of `custom-model-data`, `item-model` and `item-name`.
Also, uses a cleaner version of the Potion Handler which also supports data driven potion/mob effects.
